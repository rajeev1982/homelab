#!/bin/bash

root_folder=.

# rm -rf 		                ${root_folder}
# mkdir -p                    ${root_folder}

mkdir 				        ${root_folder}/config ${root_folder}/cache
# chown -R www-data:www-data	${root_folder}/config ${root_folder}/cache
# chmod -R 0770			    ${root_folder}/config ${root_folder}/cache
